// enums.ts

export enum Gender {
    Male = 'male',
    Female = 'female',
    Other = 'other',
}

export enum MaritalStatus {
    Single = 'single',
    Married = 'married',
    Divorced = 'divorced',
    Widowed = 'widowed',
}

export enum schoolCategory {
    TTC = 'ttc',
    TVET = 'tvet',
    GE = 'General Education',
}